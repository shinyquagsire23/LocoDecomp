# Non-idiomatic C++ cleanup catalog

Generated by `tools/lint_idiom.py --doc`. Raw offset access =
an unmodeled struct field; each fix is: model the field (src/ header
+ Ghidra struct), rewrite the access, confirm `tools/progress.py`
shows no regression. Classes: A=(char*)+off (incl. scaled `+ idx*0x18`)
B=*(T*)(p+off)
C=Unk-ptr+off  D=pad-field access  E=duplicate class definition
(consolidate to the canonical model; see the section below)
F=raw vtable-slot dispatch or any raw cast of `this` ((T*)this)->M()
(= an unmodeled virtual)
G=function-local typedef in a .cpp (hoist to the TU header)
H=memcpy/memset/memmove with no `sizeof` in the size arg (prefer
sizeof(*dst)/sizeof(T) over a bare byte count)
I=extern "C" in a .cpp (file-local extern decls — move to the
shared subsystem header so every TU + Ghidra share one name).

**Severity: A-E and H-I first** — those are non-local hazards or
correctness smells (a wrong offset/size or drifted sizeof silently
regresses OTHER TUs, or masks a wrong-size bug). F-G are
self-contained (the site's own byte-match verifies the slot);
idiom debt only, clean up last or opportunistically.

⚠ **A/B/C (raw offset) findings can NEVER be `// idiom-exempt`'d** —
only fixed (`&this->field`, or a properly-sized/positioned named
`Unk0xNN`/`UnkNN` placeholder field for an offset that doesn't map
to one field yet — see the module docstring; `offsetof` is reserved
for memcpy/memset size args, not a general substitute) or, failing
that, tagged `// TODO: idiom`.

**10 findings across 6 files** (0 untagged).

## Duplicate class definitions (2 names)

Consolidation targets — each class should end with ONE
canonical model (in its home file src/<Class>.h, or a site
marked `// idiom-exempt: canonical`); all other definitions
are class-E findings in the per-file lists below.

(none — the BigObj and DSoundChannel duplicate pairs were consolidated 2026-07-22 (v322)
when the phase2_probe files were retired: BigObj's predicates moved into src/CursorDesc.cpp,
the probe-local DSoundChannel copy was deleted as a pure duplicate.)

## src/DSound.cpp (3)

- `src/DSound.cpp:22` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name
- `src/DSound.cpp:59` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name
- `src/DSound.cpp:64` [I] `extern "C" DSound *g_pDSoundManager;  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

## src/GameNet.cpp (2)

- `src/GameNet.cpp:42` [I] `extern "C" int DAT_004a9918;  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name
- `src/GameNet.cpp:800` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

## src/TutorialWnd.cpp (2)

- `src/TutorialWnd.cpp:238` [I] `extern "C" void FUN_00407d20_SetsWindowPos_ScrollBarIdk(char bParam);  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name
- `src/TutorialWnd.cpp:300` [A] `nPresenterFrameIndex = *(unsigned short *)((char *)pDesc->paFrameEntries + (short)pDesc->wActiveF...` — suggest: &this->field (real typed member), or a properly-sized Unk0xNN/UnkNN placeholder field referenced directly -- not offsetof() (reserved for memcpy/memset size args only)

## src/DSoundChannel.cpp (1)

- `src/DSoundChannel.cpp:25` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

## src/LocoBitmap.cpp (1)

- `src/LocoBitmap.cpp:43` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

## src/SoundBank.cpp (1)

- `src/SoundBank.cpp:38` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

## src/Wav.cpp (1)

- `src/Wav.cpp:28` [I] `extern "C" {  // TODO: idiom` — suggest: move the extern decls to the shared subsystem header (extern "C" there is fine) so every TU + Ghidra share one name

